{
  "name": "qa-testpacks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "mcaddon": "rm -rf dist && mkdir -p dist/stage/qatp_bp dist/stage/qatp_rp && cp -R behavior_packs/qatp/. dist/stage/qatp_bp/ && cp -R resource_packs/qatp/. dist/stage/qatp_rp/ && cd dist/stage && zip -qr ../qatp.mcaddon qatp_bp qatp_rp"
  }
}
